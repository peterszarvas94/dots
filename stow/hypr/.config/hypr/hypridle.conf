general {
    lock_cmd = omarchy-lock-screen                         # lock screen and 1password
    before_sleep_cmd = loginctl lock-session               # lock before suspend.
    after_sleep_cmd = sleep 1 && hyprctl dispatch dpms on  # delay for PAM readiness, then turn on display.
    inhibit_sleep = 3                                      # wait until screen is locked
}

# Screensaver: hide waybar while idle animation is showing, restore on activity.
listener {
    timeout = 300
    on-timeout = pidof hyprlock || bash -lc 'omarchy-toggle-enabled screensaver-off && exit 0; pkill -9 -x waybar 2>/dev/null; omarchy-launch-screensaver'
    on-resume = pgrep -x waybar >/dev/null || omarchy-toggle-enabled waybar-off || uwsm-app -- waybar
}

listener {
    timeout = 301                      # 5min
    on-timeout = loginctl lock-session # lock screen when timeout has passed
}

listener {
    timeout = 630                                            
    on-timeout = brightnessctl -sd '*::kbd_backlight' set 0  # save state and turn off keyboard backlight
    on-resume = brightnessctl -rd '*::kbd_backlight'         # restore keyboard backlight
}

listener {
    timeout = 630                                            
    on-timeout = hyprctl dispatch dpms off                   # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r # screen on when activity is detected
}
