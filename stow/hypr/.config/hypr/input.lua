-- Personal input overrides only. Omarchy supplies the remaining defaults.
hl.config({
  input = {
    kb_layout = "us,hu",
    kb_variant = "intl,",
    kb_options = "compose:caps,shift:both_capslock_cancel,grp:alt_shift_toggle_bidir",
    repeat_delay = 200,

    touchpad = {
      natural_scroll = true,
    },
  },
})
