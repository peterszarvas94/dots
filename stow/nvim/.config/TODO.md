# TODO (nvim)

- [x] Omarchy: switch theme sync to event-based push (like mac), remove polling timer once stable.
- [x] Add/update Omarchy theme-change hook to rewrite `~/.config/omarchy/current/theme/neovim.lua`.
- [x] Reuse `~/.local/state/nvim/theme-sync-servers` and push `SyncTheme` via `nvim --server ... --remote-send`.
- [x] Keep this Omarchy-only; do not change mac flow.
