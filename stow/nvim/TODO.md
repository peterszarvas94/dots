# TODO (nvim)

- [ ] Omarchy: switch theme sync to event-based push (like mac), remove polling timer once stable.
- [ ] Add/update Omarchy theme-change hook to rewrite `~/.config/omarchy/current/theme/neovim.lua`.
- [ ] Reuse `~/.local/state/nvim/theme-sync-servers` and push `SyncTheme` via `nvim --server ... --remote-send`.
- [ ] Keep this Omarchy-only; do not change mac flow.
